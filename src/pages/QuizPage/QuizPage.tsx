import React, { PureComponent, ReactNode } from 'react'
export default class QuizPage extends PureComponent {
    render(): ReactNode {
        return(
            <>
            <h4>abc</h4>
            </>
        );
    }
}